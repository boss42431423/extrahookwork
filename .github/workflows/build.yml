name: Build extrahook

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Theos
        run: |
          git clone --recursive https://github.com/theos/theos.git $HOME/theos
          sudo mkdir -p /var/mobile
          sudo ln -sf $HOME/theos /var/mobile/theos

      - name: Download iOS 16.5 SDK
        run: |
          curl -fsSL \
            https://github.com/theos/sdks/releases/latest/download/iPhoneOS16.5.sdk.tar.xz \
            | tar -xJf - -C $HOME/theos/sdks

      - name: Build
        run: make package FINALPACKAGE=1 DEBUG=0

      - name: Upload .tipa
        uses: actions/upload-artifact@v4
        with:
          name: extrahook
          path: packages/*.tipa
          if-no-files-found: error
